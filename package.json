{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "deploy": "nuxt generate && aws s3 sync .output/public s3://mutedit.icey.one/ --profile mutedit --delete"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^5.0.4",
    "nuxt": "^3.0.0-rc.10",
    "sass": "^1.51.0",
    "sass-loader": "^12.6.0"
  },
  "dependencies": {
    "defu": "^6.1.0",
    "localforage": "^1.10.0",
    "showdown": "^2.1.0"
  },
  "overrides": {
    "ufo": "^0.8.4"
  }
}
